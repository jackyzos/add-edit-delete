# small School task

> task using Vue + Vuex + SQLITE3 + express

## Build Setup

``` bash
# install dependencies
npm install

# Run the Server on http://localhost:3000/

npm run start
node server.js
